TEST T1-01 — The Lighthouse Vision
Category: T1 (Positive) — a valid model that must traverse the COMPLETE pipeline.
Success criterion (spec Section 6.1):
  conform to metamodel -> pass EVL -> transform via ETL -> formal verification
  passes -> generate artifacts.

PIPELINE EXPECTATION
  Metamodel (EMF) ... PASS
  EVL ............... PASS (all 9 constraints)
  ETL (M2M) ......... PASS (produces a well-formed pivot NarrativeGraph)
  Verification ...... PASS (all properties below hold; no counterexample)
  EGL (M2T) ......... PASS (generates artifacts, e.g. HTML/JS story runner)

NuSMV naming: n_<scene>, has_<item>, is_accepting, is_good_ending, can_move.

WHAT THIS TEST VERIFIES
  A complete, playable story: Max climbs the Arcadia Bay lighthouse during the storm.
  Scenes: cliff_path (start) -> storage_hut -> lighthouse_base -> lamp_room ->
  ending_clarity (GOOD); plus a dice climb (lighthouse_base) to outer_platform ->
  ending_safe_together (NEUTRAL), with storm_ledge looping back to lighthouse_base.
  Mechanics exercised: item gate (lighthouse_key), dice roll, a SET_VARIABLE effect
  (vision_understood), an escapable loop, two ending types.

PROPERTIES CHECKED (NuSMV) AND EXPECTED RESULT
  EF node = n_ending_clarity ................. TRUE  (GOOD ending reachable)
  EF node = n_ending_safe_together ........... TRUE  (NEUTRAL ending reachable)
  EF is_good_ending .......................... TRUE
  AG (EF is_accepting) ....................... TRUE  (no softlock; loop is escapable)
  AG (!is_accepting -> can_move) ............. TRUE  (no deadlock)
  G (node = n_lamp_room -> has_lighthouse_key) TRUE  (locked scene never entered without key)

EXPECTED RESULT
  All constraints and properties hold. No counterexample. Artifacts generate successfully.

EVIDENCE 
  -- specification EF node = n_caught_by_security  is true
  -- specification EF node = n_ending_proof  is true
  -- specification AG (!is_accepting -> can_move)  is true
  -- specification AG (EF is_accepting)  is true
  -- specification EF is_good_ending  is true
  -- invariant !(node = n_sealed_cabinet & !has_keycard)  is true
